'use client'
import { Menu, X } from 'lucide-react'
import { useState } from 'react'
import DelFioreExperience from '../components/DelFioreExperience'

export default function Home(){const [open,setOpen]=useState(false);const go=(id:string)=>{setOpen(false);document.getElementById(id)?.scrollIntoView({behavior:'smooth'})};return <main className="experience-page"><header className="experience-nav"><button className="logo" onClick={()=>go('hero')}><small>VIAZUL MMC</small><strong>Del Fiore</strong><em>KACOTTA PENDİRİ</em></button><nav className={open?'open':''}><button onClick={()=>go('hero')}>Əsas səhifə</button><button onClick={()=>go('collection')}>Məhsullar</button><button onClick={()=>go('story')}>Hekayəmiz</button><button onClick={()=>go('recipes')}>Reseptlər</button><button onClick={()=>go('locator')}>Mağazalar</button><button onClick={()=>go('footer')}>Əlaqə</button></nav><div className="nav-right"><span>AZ⌄</span><button className="menu" onClick={()=>setOpen(v=>!v)} aria-label="Menyu">{open?<X/>:<Menu/>}</button></div></header><DelFioreExperience/><footer id="footer"><button onClick={()=>go('hero')} className="logo"><small>VIAZUL MMC</small><strong>Del Fiore</strong><em>KACOTTA PENDİRİ</em></button><p>Instagram &nbsp; YouTube &nbsp; TikTok</p><small>© 2026 Viazul MMC</small></footer></main>}
